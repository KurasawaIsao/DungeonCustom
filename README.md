# 不思議のダンジョンCustom

C++ / DirectX 11 で制作している、ローグライク形式の 3D ダンジョンゲームです。
ターン制の移動・戦闘、ランダムダンジョン生成、アイテム、罠、敵 AI、ショップやモンスターハウスなどの特殊部屋を実装しています。

## 主な実装内容

- 部屋と通路を組み合わせるダンジョン自動生成
- プレイヤー、敵、仲間ユニットを扱うターン制システム
- 追跡、巡回、逃走、距離維持などの敵 AI
- アイテム、杖、食料、状態異常、罠のデータ駆動管理
- ショップ、モンスターハウス、階段、祠などのマップオブジェクト
- ImGui を使ったダンジョン、マップ、出現テーブル編集機能
- DirectX 11 による 3D モデル、UI、ミニマップ、エフェクト描画

## 使用技術

- C++
- DirectX 11
- Visual Studio 2022
- ImGui
- nlohmann/json
- Assimp

## ビルド方法

1. Visual Studio 2022 で `Project1.sln` を開きます。
2. 構成を `Debug` または `Release`、プラットフォームを `x64` に設定します。
3. ビルドして実行します。

## 内部コード概要
シングルトンについて
MapManagerやUnitManagerのポインタ参照や、MessageLogやLightManagerといった実体参照がありますが、
すべてのシーンで使う予定があり、常駐する必要があるかないかで設計方法としてポインタと実体を分けています。

## 補足
`.vs/`、`x64/`、中間オブジェクト、ユーザー個別設定などの生成ファイルは Git 管理から除外しています。
ソースコード、ゲームデータ、シェーダー、モデル・テクスチャなど、プロジェクトの確認に必要なファイルを保存対象にしています。

ゲーム進行関係:

-敵AI切り替えについて
 主にEnemyのApplyData()にて行っていますが、
 データベースにおいて設定したAIの内容に基づき、Enemyが所持しているUnitAIに
 対応したAI(UnitAI)を入れます。
 以降、基本は入れたAI->Update(*this, MapData)で振る舞いを変えて処理します。

-マップ管理について
 MapDataにてマップの状態に関する操作が行えますが、
 MapData自体はMapManagerの保護下にあるため、
 MapManager::GetCurrentMap()->と経由して操作することになります。
 また、ダンジョンのデータや現在の階層データもMapManagerが保持するためここでもMapManagerを経由します。

-ターン管理について
 ターン進行はTurnManagerが担当します。
 基本的な流れは、PlayerTurnでプレイヤーの入力と行動を処理し、
 行動が完了したらEnemyAction、EnemyMove、MoveResolutionを経由して
 再びPlayerTurnへ戻る形になります。
 敵や仲間の行動は、攻撃などの行動フェーズと移動フェーズを分けて処理しています。
 これにより、倍速や三倍速のユニットが持つ行動回数・移動回数を
 個別に扱えるようにしています。
 また、移動アニメーション中や投擲物の処理中は次のフェーズへ進めず、
 演出が完了してからターンを進めます。
 そのため、ゲーム内部のマス移動と画面上の見た目がずれないようになっています。

-ユニット管理について
 プレイヤー、敵、仲間の参照管理はUnitManagerが行います。
 UnitManagerは現在存在している敵や仲間の一覧を保持し、
 指定座標にユニットがいるか、隣接する敵がいるか、
 プレイヤーや敵への参照取得などを担当します。
 移動や攻撃時にはUnitManager::GetUnitAt()を使って、
 移動先に他ユニットがいないか、攻撃対象が存在するかを確認します。
 これにより、各Unitが個別に全ユニットを探すのではなく、
 UnitManagerを経由して盤面上のユニット状態を確認する形にしています。

-ダンジョン開始と階層遷移について
 DungeonSceneの初期化時にMapManagerへDungeonDataを渡し、
 MapManager::StartDungeon()で現在階層のFloorDataをもとにマップ生成を行います。
 生成後は敵、アイテム、プレイヤーの配置を行い、
 MapRendererにBuildを呼ぶことで画面表示へ反映します。
 階段や効果によって次の階層へ進む場合は、MapManager::RequestNextFloor()を呼びます。
 この中で現在階層番号を進め、演出としてFloorTransitionUIを挟んでから
 MapManager::ChangeFloor()で新しい階層を生成します。
 階層移動時には、現在の敵やマップ上のオブジェクトを破棄し、
 次のFloorDataに基づいて新しいMapDataを作成します。
 ダンジョンの最終階層を超えた場合はResultシーンへ遷移します。

-マップ生成と配置について
 MapManagerが現在階層のFloorDataを参照し、
 ランダム生成か固定マップ読み込みかを判断します。
 ランダム生成の場合はMapGeneratorを使用し、
 固定マップの場合はMapLoaderでJSONからMapDataを復元します。
 敵、アイテム、罠などの配置数や出現内容はFloorDataに設定された値や
 各種出現テーブルIDをもとに決定します。
 そのため、階層ごとの難易度や出現傾向はFloorData側で調整でき、
 実際の生成処理はMapGenerator側に集約しています。

-移動後処理について
 ユニットが移動した後の処理はMapManager::AfterUnitMoved()で行います。
 主にプレイヤーが移動した後、現在いる部屋、店内状態、足元のオブジェクト、
 モンスターハウスの発見などを確認します。
 足元にアイテム、罠、祠などのMapObjectがある場合は、
 そのオブジェクトのOnStepped()を呼びます。
 これにより、プレイヤー側ではなく各オブジェクト側に
 踏まれた時の処理を持たせる形にしています。

-アイテム・罠などの効果処理について
 アイテム使用、罠の発動、スキルなどの効果はEffectBaseを継承したクラスで処理します。
 効果を発動する際はEffectContextに使用者、対象、座標、方向、
 効果の種類やランクなどを入れてApply()を呼びます。
 これにより、回復、ダメージ、ワープ、吹き飛ばし、AI変更などの処理を
 共通の形で扱えるようにしています。
 アイテムや罠は効果の発動元が違うだけで、
 最終的な処理はEffectBase系のクラスに任せる設計になっています。

-自然湧きについて
 ターンが一定数進むとTurnManager::SpawnEnemy()によって敵の自然湧きが行われます。
 通常時は一定ターンごとに敵を追加し、
 店での泥棒状態など特殊な状態では湧き間隔や上限を変えています。
 自然湧きの敵も現在階層のFloorDataを参照して決定されるため、
 階層ごとの敵出現テーブルに沿った敵が追加されます。

エディタ関係:

-エディタシーンについて
 EditorSceneでは、編集用のCamera、MapRenderer、MapEditorを生成します。
 実際の編集処理はMapEditorが中心となって行い、EditorScene自体は
 編集用のシーンを用意する役割に留めています。

-MapEditorについて
 MapEditorは、編集中のMapDataとDungeonDataを保持し、
 ImGuiを使って地形編集、オブジェクト配置、ダンジョン階層設定、
 出現テーブル編集を行います。
 ゲーム本編ではMapDataはMapManagerの管理下にありますが、
 エディタでは編集中の一時データとしてMapEditorがMapDataを直接保持しています。
 これは、エディタ上ではまだゲーム進行中の正式なマップではなく、
 保存前の編集データとして扱うためです。

-地形編集について
 地形編集はMapGeometryEditorが担当します。
 床、壁のタイル変更や、部屋情報の更新を行い、
 変更後はMapRendererにBuildを呼ぶことで見た目を同期します。
 MapRendererはあくまで表示用であり、
 マップの状態そのものはMapData側に保持されます。
 そのため、編集内容を保存する際もMapRendererではなくMapDataを参照します。

-オブジェクト配置について
 アイテム、罠、祠などの配置はMapObjectPlacerが担当します。
 配置されたオブジェクトはEditorPlacedObjectとして保持し、
 種類、ID、座標、個数や杖の回数などの追加情報を持たせています。
 固定マップとして保存する場合は、
 タイル情報だけでなく配置オブジェクトもまとめてJSONに出力します。
 読み込み時にはJSONからMapDataを復元し、配置オブジェクトも再生成します。

-マップファイル管理について
 マップデータの保存と読み込みはMapFileManagementを経由します。
 MapEditor側ではファイル名や保存先の選択を行い、
 実際のJSON出力・入力処理はMapFileManagementに任せています。
 保存先は用途ごとに分けており、
 通常の固定マップはDungeonData\\EdittedMapData、
 部屋パーツはDungeonData\\EdittedRoomDataに保存します。
 これにより、1フロア全体として使うマップと、ランダム生成中に差し込む部屋パーツを区別しています。

-ダンジョン階層編集について
 ダンジョン全体の構造はDungeonDataとして管理します。
 DungeonStructureEditorでは、階層ごとのFloorDataを編集し、
 マップサイズ、部屋数、敵・アイテム・罠の出現テーブル、
 視界距離、特殊部屋の出現率などを設定します。
 各階層のマップ生成方法は、ランダム生成と固定マップのどちらかを選べます。
 ランダム生成の場合はFloorDataの設定をもとにMapGeneratorがマップを作成し、
 固定マップの場合は指定されたJSONファイルをMapLoaderが読み込みます。
 また、複数階層に同じ設定を反映しやすくするため、
 選択した階層の内容を他の階層へ一括適用する機能も用意しています。
 これにより、似た構造の階層をまとめて作り、必要な部分だけ後から調整できます。

-出現テーブル編集について
 敵、アイテム、罠の出現内容はSpawnTableEditorで編集します。
 各テーブルはIDで管理し、FloorDataには使用するテーブルIDだけを保存します。
 実際の生成時には、MapManagerが現在階層のFloorDataを参照し、
 EnemyTableDatabase、ItemTableDatabase、TrapTableDatabaseから
 対応する出現テーブルを取得します。
 これにより、階層データ側は「どのテーブルを使うか」だけを持ち、
 具体的な出現内容は各データベース側で管理できます。

-テストプレイについて
 エディタからは作成したダンジョンデータを指定してテストプレイできます。
 テストプレイ開始時にはDataReferenceに次に読み込むダンジョンIDや
 エディタテスト中であることを記録し、DungeonSceneへ遷移します。
 DungeonScene側ではDataReferenceの情報を参照してDungeonDataを読み込み、
 MapManagerに渡して通常のゲーム進行と同じ流れでダンジョンを開始します。
 そのため、エディタで作成したデータを本番に近い状態で確認できます。
 また、テストプレイ時にはランダムシード(Use Generation Seed)を固定するか、
 毎回変化させるかを選べるようにしています。
 固定すれば同じ構造を再確認しやすくなり、
 ランダム化すれば生成パターンの確認に使えます。

-生成AIの利用について
 本プロジェクトでは、設計方針の整理、コード構成の見直し、コメント文や内部資料の作成補助、
 不具合原因の調査補助などに生成AIを利用しています。
 ただし、生成AIの出力をそのまま採用するのではなく、
 実際の仕様、既存コードとの整合性、ビルド結果、動作確認を開発者側で確認したうえで反映しています。
 特にゲーム進行、マップ生成、ターン管理、ユニット管理、敵及び味方AIなどの主要処理については、
 生成AIの提案を参考にしつつ、最終的な実装判断と調整は開発者が行っています。


